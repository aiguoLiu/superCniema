import Vue from 'vue'
import CityBox from './CityBox'


export const cityBox = (function() {

    let defaults = {
        //默认值
        title: '',

    };

    return function (opts) { 
        //配置参数
        

     };

})();